# Hugging Face access token, used only for gated/private datasets
# (japanese-honorifics is public, so this can stay empty/unset).
# Get one at https://huggingface.co/settings/tokens
HF_TOKEN=INSERT_YOUR_TOKEN_HERE